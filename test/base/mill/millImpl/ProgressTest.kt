package base.mill.millImpl

// board: 000000... x move -> 00000...
class ProgressTest {

    fun testEmpty() {
        assertProgress(EMPTY_BOARD, 0, 1)
    }

    /*------------------------------------------------------------------------------------------------------------------
    private helper
    ------------------------------------------------------------------------------------------------------------------*/
    private fun assertProgress(board: Long, move: Int, ) {

    }
}